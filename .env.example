# This file is only for non-secret local settings. Provider credentials must
# be imported into the session Secret Service with `vibe secrets import`.

# E3 reviews expire after this many seconds.
VIBEOS_REVIEW_TTL_SECONDS=600
